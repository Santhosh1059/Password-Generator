{
  "name": "password-generator",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "jest --color",
    "lint": "eslint . --ext .js",
  "build": "esbuild src/entry.js --bundle --sourcemap --outfile=dist/bundle.js --platform=browser --format=esm",
  "build:watch": "esbuild src/entry.js --bundle --sourcemap --outfile=dist/bundle.js --platform=browser --format=esm --watch"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "jest": "^29.0.0",
    "esbuild": "^0.18.0"
  }
}
